# Composite risk model (v2)

**Location:** All formulas and weights are in **`risk_scoring_engine.py`** at the top of the file (section “MODEL (v2)”). The data generator imports the same functions from that file.

**Bands:** Low 0–33, Medium 34–66, High 67–100.

See the module docstring in `risk_scoring_engine.py` for an honest checklist against the full SRS (what is fully implemented vs partial vs not implemented).
